seed: 42
output_dir: runs/cifar100_vit_patch4

dataset:
  name: cifar100
  data_dir: ./data
  image_size: 32
  batch_size: 128
  num_workers: 2
  val_ratio: 0.1
  seed: 42

model:
  name: simple_vit
  image_size: 32
  patch_size: 4
  num_classes: 100
  embed_dim: 192
  depth: 6
  num_heads: 3
  mlp_ratio: 4.0
  dropout: 0.1

training:
  epochs: 80
  lr: 0.0003
  weight_decay: 0.05
  label_smoothing: 0.1

